spec_version: "0.4.0"
name: writer-agent
version: 1.0.0
description: Takes audit findings and writes a polished GitHub issue

model:
  preferred: "groq:meta-llama/llama-4-scout-17b-16e-instruct"
  fallback: []

tools:
  - read
  - write

runtime:
  max_turns: 5
  timeout: 120
